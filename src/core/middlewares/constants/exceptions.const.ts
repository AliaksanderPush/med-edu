export const ConvertOffsetLimitException = {
  INVALID_SKIP_FORMAT: (input: unknown) => `Invalid skip format: ${input}`,
  INVALID_TAKE_FORMAT: (input: unknown) => `Invalid take format: ${input}`,
};
